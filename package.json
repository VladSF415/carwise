{
  "name": "carwise-server",
  "version": "2.0.0",
  "description": "CarWise Railway server (Postgres/Prisma)",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && node index.js",
    "install-server": "cd server && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
